spring:
  application:
    name: hub-service

  datasource:
      url: jdbc:postgresql://db-hub.c5uuwiy2enpx.ap-southeast-2.rds.amazonaws.com:5432/postgres
      username: master
      password: 'o90o90o90!'
      driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        temp:
          use_jdbc_metadata_defaults: false

  springdoc:
    swagger-ui:
      path: /swagger-ui.html

logging:
  level:
    org.hibernate.SQL: debug              # 실행되는 SQL 출력
    org.hibernate.orm.jdbc.bind: trace    # ? 자리에 들어가는 파라미터 값 출력